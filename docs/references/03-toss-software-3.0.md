# 소프트웨어 3.0 시대를 맞이하며

> Source: [토스테크 블로그](https://toss.tech)
> Author: 김용성 (토스페이먼츠 Node.js Developer)
> Date: 2026-01-26

## Software 3.0 시대란?

Andrej Karpathy의 소프트웨어 진화 3단계:

| 단계 | 특징 | 핵심 |
|------|------|------|
| **1.0** | Python, Java, C++ | **How**를 코드로 작성 |
| **2.0** | 딥러닝, 신경망 | 데이터 → 학습 → 가중치가 프로그램 |
| **3.0** | LLM, 자연어 | **What**을 말하면 됨, 프롬프트가 프로그램 |

> "Software 3.0 is eating 1.0/2.0" - Karpathy

## Harness: LLM을 쓸모있게 만드는 것

**Harness** = 마구(馬具). 말의 힘을 인간이 활용할 수 있게 해준 도구.

LLM도 마찬가지. 그 자체만으로는 제어하거나 활용하기 어려움.

| LLM 한계 | Harness 역할 |
|----------|-------------|
| Context window 제한 | Memory 관리 |
| 환각(Hallucination) | Fact grounding, RAG |
| 도메인 지식 부족 | Knowledge Base |
| 상태 관리 불가 | Session, Orchestration |
| 외부 시스템 접근 불가 | Tool, MCP |

**Claude Code도 Harness다** - Claude 모델을 실제로 일할 수 있는 에이전트로 만들어주는 도구.

## 레이어드 아키텍처와의 매핑

```
Claude Code 개념     ≈     전통 아키텍처
────────────────────────────────────────
Slash Command       =     Controller (@RestController, router)
Sub-agent           =     Service Layer (여러 컴포넌트 조율)
Skills              =     Domain Component (SRP)
MCP                 =     Infrastructure/Adapter (Repository Pattern)
CLAUDE.md           =     package.json (잘 변하지 않는 원칙)
```

### 각 레이어 상세

**Slash Command = Controller**
```
/review PR-1234
→ review 워크플로우 트리거
→ 적절한 Sub-agent와 Skill 조합 실행
```

**Sub-agent = Service Layer**
- 여러 Skill을 조합하여 워크플로우 완성
- 별도의 Context를 가짐 (별도 스레드처럼 독립적)

**Skills = Domain Component (SRP)**
- 단일 책임 원칙을 따르는 기능 단위
- "코드 리뷰하기", "테스트 생성하기", "문서 작성하기"

**MCP = Infrastructure/Adapter**
- 외부 시스템과의 연결 (DB, API, 파일 시스템)
- 내부 로직이 외부 구현에 의존하지 않도록 추상화

**CLAUDE.md = package.json**
- 잘 변하지 않는 원칙 (기술 스택, 코딩 컨벤션, 빌드 명령어)
- *자주 수정하고 있다면, 그 내용은 거기 있으면 안 됨*

## 안티패턴도 그대로 적용됨

| 전통 안티패턴 | 에이전트 버전 | 증상 |
|--------------|--------------|------|
| God Class | God Skill | 3000줄, 모든 걸 다 처리 |
| Spaghetti Code | Spaghetti CLAUDE.md | 구조 없이 모든 지시사항 뒤섞임 |
| Tight Coupling | MCP 없는 하드코딩 | API 변경 시 전체 수정 |
| Leaky Abstraction | Sub-agent가 MCP 내부 구현을 앎 | 추상화 경계 붕괴 |
| Circular Dependency | Skill 간 순환 호출 | A→B→C→A 무한 루프 |

### 코드 스멜
- **Feature Envy**: Skill이 다른 Skill 데이터를 과도하게 참조
- **Duplication**: 비슷한 프롬프트가 여러 Skill에 복사됨
- **Long Method**: Sub-agent 하나가 10개 Skill을 연속 호출

## 결정적인 차이: 에이전트는 질문할 수 있다

전통적인 서비스 레이어:
```java
// 모든 분기가 미리 정의되어야 함
if (inventory.check() < request.getQuantity()) {
    throw new OutOfStockException();  // 정해진 예외
}
```

에이전트 (Human-in-the-Loop):
```
Request → Agent → 작업 진행 중...
                      ↓
                 🤔 불확실한 상황 발생
                      ↓
                 "A와 B 중 어떤 걸 원하세요?"
                      ↓
User Answer → "A로 해줘"
                      ↓
                 계속 진행 → 완료
```

> **Exception이 Question으로 바뀌는 것**

### 언제 질문하고, 언제 알아서 할 것인가

| 질문해야 할 때 | 알아서 해야 할 때 |
|---------------|-----------------|
| 되돌리기 어려운 작업 | 안전하게 반복 가능한 작업 |
| 정답이 없는 선택지 | 이미 합의된 컨벤션 |
| 비용/리스크가 큰 결정 | 되돌리기 쉬운 작업 |

> "좋은 에이전트는 '언제 질문할지'를 아는 에이전트"

## 토큰 관리: 메모리처럼 생각하라

- Context Window = 작업 메모리
- 토큰 사용량 = 메모리 점유율

| 요소 | 대략적인 토큰 |
|------|-------------|
| CLAUDE.md | 500~2,000 |
| Skill 하나 | 300~1,500 |
| 대화 히스토리 | 누적 |
| MCP 응답 | 가변 |

### 토큰 아끼는 방법: 결정적 로직을 scripts로 분리

```markdown
# 안티패턴: LLM이 매번 해석
"브랜치명은 feature/JIRA-{티켓번호}-{설명} 형식으로..."

# 권장: scripts가 캡슐화
./scripts/create-branch.sh JIRA-1234 "로그인 기능"
→ feature/JIRA-1234-login-feature
```

## Skill 설계: Progressive Disclosure

```
# 안티패턴: Skill 폭발 (20개 Description이 항상 Context 점유)
.claude/skills/
├── review-naming/
├── review-types/
├── review-complexity/
└── ... (15개 더)

# 권장: Facade 패턴
.claude/skills/code-review/
├── SKILL.md              # 진입점만 (로드됨)
├── references/           # 필요할 때만 로드
│   ├── naming-rules.md
│   └── security-checklist.md
└── scripts/
```

## 1.0 개발자가 3.0으로 가는 길

### 버릴 것
- "모든 로직을 명시적으로 작성해야 한다"는 강박
- 모든 예외 상황을 미리 정의하려는 시도
- LLM을 '똑똑한 자동완성' 정도로만 보는 시각

### 가져갈 것
- 레이어 분리, 단일 책임 원칙, 추상화
- 의존성 관리, 인터페이스 설계
- 테스트 가능성, 디버깅 전략
- 코드 리뷰, 점진적 개선

> "도구는 바뀌었지만, 좋은 설계의 원칙(응집도, 결합도, 추상화)은 그대로"

## 핵심 메시지

> "Start building by refactoring your mindset."

- MCP 설계할 때 → Adapter Pattern
- Skill 만들 때 → SRP
- Sub-agent 구성할 때 → Service Layer

**여러분이 가진 아키텍처 지식이 곧 최고의 에이전트를 만드는 기반**

## References

- [Y Combinator AI Startup School - Karpathy](https://www.youtube.com/watch?v=LCEmiRjPEtQ)
- [claude-hud](https://github.com/anthropics/claude-code/tree/main/plugins/claude-hud)
- [Claude Code 공식 문서](https://docs.anthropic.com/claude-code)
