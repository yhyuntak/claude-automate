# Session: 2026-01-23

## 맥락
피드백 리뷰 후 `/start-work` 통합 워크플로우 커맨드 설계 및 구현. 사용자 피드백의 백로그 설명 컬럼과 /start-work 아이디어를 기반으로 전체 작업 시작 프로세스를 단일 커맨드로 통합.

## 작업 요약
- 피드백 리뷰: 백로그 설명 컬럼 기능 (이미 구현됨) 및 /start-work 아이디어 확인
- `/start-work` 커맨드 설계: 세션 요약 → 백로그 표시 → worktree 설정 → 작업 선택 → 환경 구성의 통합 플로우
- 구현 완료: `/commands/start-work.md` 파일 신규 생성으로 커맨드 문서화

## 결정사항
- **worktree 경로**: `../{project}-{branch}` 형식 (예: `../claude-automate-feature-x`)
- **브랜치명**: 백로그 slug 그대로 사용 (예: `feature-x`, `fix-y`)
- **작업 위치**: Claude가 worktree로 cd하여 작업 (물리 터미널은 main 브랜치 유지)
- **기존 커맨드**: `/session-start`, `/backlog` 유지 및 활용
- **통합 구조**: 단일 `/start-work` 커맨드가 전체 초기화 담당

## 미완료/TODO
- [ ] `/start-work` 커맨드 구현 코드 작성 (TypeScript)
- [ ] 사용자 프롬프트 및 질문 템플릿 최종화
- [ ] worktree 생성 및 관리 로직 구현
- [ ] 기존 `/session-start` 플로우와의 통합 확인
- [ ] E2E 테스트: start-work → backlog 선택 → 작업 시작 전체 플로우 검증

## 다음 세션 제안
1. `/start-work` 커맨드 핵심 로직 구현 (worktree 생성, 브랜치 관리)
2. 사용자 인터랙션 및 에러 핸들링 추가 (worktree 충돌, 브랜치 존재 확인)
3. 기존 세션 컨텍스트 시스템과 start-work의 통합 방식 정의
4. 통합 테스트 진행 및 문서 업데이트
